#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../osmtogeojson/osmtogeojson" "$@"
  ret=$?
else 
  node  "$basedir/../osmtogeojson/osmtogeojson" "$@"
  ret=$?
fi
exit $ret
