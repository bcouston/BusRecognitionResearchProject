#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../geojson-numeric/geojson-numeric" "$@"
  ret=$?
else 
  node  "$basedir/../geojson-numeric/geojson-numeric" "$@"
  ret=$?
fi
exit $ret
